# ЧТО В ЭТОЙ ПАПКЕ

**Путь:** `C:\Users\бб\Desktop\MEDUK_SAYT_AKTUALNAYA_VERSIYA`

Всё лежит плоско, БЕЗ вложенных папок — zip-файлы нейросеть не читает, поэтому архив `admin (1).zip` распакован сюда.

## Состав

1. **46 файлов актуальной (живой) версии сайта meduk.ru** — распакованы из архива админки. Вместо папок — префиксы в именах:
   - `admin-*` → было в папке `admin/` (index.php, lib.php, retention.php, .htaccess)
   - `assets-*` → было в папке `assets/` (иконки, эмблемы, логотипы, картинки)
   - `assets-fonts-*` → было в папке `assets/fonts/` (Manrope, Unbounded, лицензии OFL)
   - остальные файлы (index.html, app.js, audio.js, styles.css, sw.js, .htaccess, collect.php, pulse.php и др.) — корень сайта

2. **`MEDUK_MASTER_PLAN_VSE_15_VERSIJ.md`** — массивный сводный документ: все 15 версий плана развития (v1–v15), исправления v16, текущее состояние, контракты C0–C6, шаблоны контента, полный перечень задач «сделать всё» и запреты. Этот файл самодостаточен — по нему другая нейросеть может делать всю работу.

## Для нейросети-исполнителя

- Прочитать `MEDUK_MASTER_PLAN_VSE_15_VERSIJ.md` (это план и инструкция).
- Код актуальной версии — файлы рядом в этой же папке (префиксы = бывшие папки).
- Рабочая версия разработки — `C:\meduk` (код в `C:\meduk\site`, приватное в `C:\meduk\server`, тесты в `C:\meduk\tests`).

## Важно

- Публичный meduk.ru не изменён; платные/внешние действия (покупки, регистрации, DNS, письма, платежи, деплой) — только по явному решению владельца.
- Не публиковать: `C:\meduk\.local`, `C:\meduk\.tools`, `C:\meduk\backups`, ключи, пароли.